fn main() {
    let mut x: u8 = 10; //mut allows for mutation
    // unsigned: positive, signed: positive or negative
    // 8 -> 32: 255 -> a lot bigger number
    // f: float
    println!("x is {}", x);
    let y: f32 = 3.123456789; //truncated due to 32 bits
    println!("y is {}", y);
}
